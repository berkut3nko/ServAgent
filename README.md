# ServAgent

**ServAgent** — це комплексний голосовий асистент, розроблений для обробки української мови. Проєкт поєднує в собі технології розпізнавання мовлення (STT), аналізу тексту, пошуку відповідей на основі контексту (QA) та синтезу мовлення (TTS).

Система здатна слухати користувача, виділяти запитання з потоку мовлення, знаходити відповіді у визначеній базі знань та озвучувати їх.

## 🚀 Основні можливості

* **Розпізнавання мовлення (STT):** Перетворення голосу в текст у реальному часі за допомогою моделі `Yehor/w2v-bert-uk-v2.1`.
* **Виявлення запитань:** Аналіз транскрибованого тексту для виокремлення запитань за допомогою ключових слів та регулярних виразів.
* **Пошук відповідей (QA):** Використання моделей Transformer (`timpal0l/mdeberta-v3-base-squad2` або `robinhad/ukrainian-qa`) для знаходження точної відповіді у наданому текстовому контексті.
* **Синтез мовлення (TTS):** Озвучування відповіді користувачеві з використанням моделі `facebook/mms-tts-ukr` (VITS).
* **Асинхронна робота:** Використання `asyncio` для паралельного запису, обробки та відтворення звуку без затримок.

## 📂 Структура проєкту

Проєкт розділено на кілька модулів, кожен з яких відповідає за окремий етап обробки:

* **`RecognizeFormatAnswerTTS/`**: Основний модуль. Містить `main.py`, який об'єднує всі компоненти (STT -> QA -> TTS) в єдиний пайплайн.
* **`CompileModel/`**: Скрипти для оптимізації та квантування (8-bit) моделей для зменшення використання пам'яті.
* **`GenerativeModel/`**: Модуль, що відповідає за логіку пошуку відповідей (Question Answering) на основі контексту.
* **`SpeechT5/`**: Експерименти з моделями синтезу мовлення (TTS).
* **`w2v-xls-r-uk/`**: Модуль для тестування та налаштування автоматичного розпізнавання мовлення.
* **`PunctuationRestoration/`**: Інструменти для відновлення пунктуації та форматування розпізнаного тексту.
* **`SpeechSynthesizer/`**: Окремий приклад реалізації синтезу мовлення на C# (.NET 9.0).

## 🛠 Вимоги до системи

Для коректної роботи проєкту необхідно встановити Python 3.8+ та наступні бібліотеки. Рекомендується використання GPU (CUDA) для пришвидшення роботи нейромереж.

### Встановлення залежностей

Створіть віртуальне середовище та встановіть необхідні пакети:

```bash
pip install torch torchvision torchaudio --index-url [https://download.pytorch.org/whl/cu118](https://download.pytorch.org/whl/cu118)
pip install transformers sounddevice soundfile numpy scipy accelerate bitsandbytes